package game;

import org.newdawn.slick.Graphics;

/**
 * Interface for a RenderObject - an object that has no game logic function
 * and is only for display
 * @author prashan
 *
 */

public interface RenderObject {

	public void draw(Graphics g);
}
